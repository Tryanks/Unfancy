interface:
  display_name: "Unfancy"
  short_description: "Keep UI copy plain, exact, and product-native."
  default_prompt: "Use $unfancy to review or rewrite UI copy so labels, statuses, actions, warnings, helper text, and empty states stay plain, exact, and semantically correct."
